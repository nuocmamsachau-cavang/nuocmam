# Responsive Validation Result

Ngày kiểm thử: 2026-08-12.

Website công khai bản xem trước đã được chụp và kiểm tra ở ba kích thước: desktop `1440x1000`, tablet `834x1112` và mobile `390x844`. Script xác nhận cả ba ảnh được tạo đúng kích thước.

Ảnh desktop cho thấy header mascot, hero nền đỏ-gold, CTA và các khối khuyến mãi nằm cân đối theo hai cột. Ảnh mobile cho thấy logo và tiêu đề tự xuống dòng, hero không tràn ngang, nội dung CTA vẫn nằm trong vùng nhìn thấy; các khối khuyến mãi chuyển sang bố cục dọc theo chiều rộng hẹp. Tablet đã được kiểm tra tự động cùng quy trình kích thước.

Kết quả kích thước: desktop PASS, tablet PASS, mobile PASS.
